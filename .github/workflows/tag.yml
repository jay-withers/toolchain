name: tag

on:
  push:
    branches: [main]

jobs:
  # Bump the semver tag from the conventional commits since the last release
  # (and create a matching GitHub release) via the shared release template.
  tag:
    permissions:
      contents: write
    uses: jay-withers/template-pipelines/.github/workflows/release.yml@bd8296ab9460020813f69f39c9b8e5080cf1b674 # 0.1.1
