name: tag

on:
  push:
    branches: [main]

jobs:
  # Bump the semver tag from the conventional commits since the last release
  # (and create a matching GitHub release) via the shared release template.
  tag:
    permissions:
      contents: write
    uses: jay-withers/template-pipelines/.github/workflows/release.yml@492843dcd47b10f45ad3ee533781c484c6e07f3c # v0.2.3
